import 'package:flutter/material.dart';

class ConsultaOpcoes extends StatelessWidget {
  const ConsultaOpcoes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text('Pagina 1 de Consulta'),
    );
  }
}
